import Layout from "../components/loginRegisterPage/Layout";
import LoginRegisterInput from "../components/Inputs/LoginRegisterInput";

const RegisterPage = () => {
  return (
    <Layout label="Register">
      <LoginRegisterInput />
    </Layout>
  );
};

export default RegisterPage;
